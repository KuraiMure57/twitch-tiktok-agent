name: Prueba publicación TikTok

on:
  workflow_dispatch:

jobs:
  tiktok-publish-test:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install requests

      # Creamos tus metadatos de prueba igual que antes
      - name: Crear metadata de prueba
        run: |
          cat > metadata.json <<'EOF'
          {
            "title": "Prueba de publicación TikTok - KuraiMure57",
            "hashtags": [
              "#Gaming",
              "#Twitch",
              "#KuraiMure57"
            ]
          }
          EOF

      # === PASO MODIFICADO: Inyectamos el SESSION_ID ===
      - name: Publicar vídeo de prueba en TikTok
        env:
          TIKTOK_SESSION_ID: ${{ secrets.TIKTOK_SESSION_ID }}
        run: |
          python src/tiktok_uploader.py \
            src/tiktok_test.mp4 \
            metadata.json

      # Este paso lee el archivo final para mostrar el resumen en consola
      - name: Mostrar resultado de publicación
        if: always()
        run: |
          if [ -f tiktok_result.json ]; then
            python - <<'PY'
          import json

          with open(
              "tiktok_result.json",
              "r",
              encoding="utf-8",
          ) as file:
              result = json.load(file)

          safe_result = {
              "status": result.get("status"),
              "publish_id": result.get("publish_id"),
              "post_ids": result.get("post_ids"),
              "caption": result.get("caption"),
          }

          print(
              json.dumps(
                  safe_result,
                  ensure_ascii=False,
                  indent=2,
              )
          )
          PY
          else
            echo "No se generó tiktok_result.json."
          fi
